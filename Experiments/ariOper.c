#include <stdio.h>
#include <math.h>

int main(){
    //arithmetic operators
    // + addition
    // - substraction
    // * multiplication
    // / division
    // % modulus
    // ++ increment 
    // -- decrement 
    int x = 5;
    int y = 2;
    
    float z = x * y;
    //float z = x / (float) y;
    printf("%f", z);
    // we can also use these arithemtic operators in the following way: x = x + 1 (x+=1)
}